---
description: Greet the user and confirm the example plugin is loaded.
---

Say hello to the user, confirm that the `example-plugin` is installed and working,
and briefly list what this plugin provides (a command, an agent, and a skill).

If the user passed any arguments after the command, incorporate them into the greeting.
